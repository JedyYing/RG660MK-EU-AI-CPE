# RG660MK-EU AI CPE 迁移设计文档

- 版本：2026-08-22 执行版
- 适用对象：RG660MK-EU、Ubuntu 主机、Immich、PicoClaw
- 参考原件：
  - `references/RG660MK-EU_AI_CPE_SG560D_USB算力棒迁移设计_V2.1_20260820.pdf`
  - `references/RG660MK_AI_CPE_PicoClaw_Migration_Design.md`

## 1. 目标

在不破坏 RG660MK 现有系统与 CPE 基础能力的前提下，先完成：

1. RG660MK 上 PicoClaw 可运行。
2. RG660MK 上可完成一次真实文本请求。
3. Ubuntu 主机侧 Immich 可被 PicoClaw 后续接入。
4. 全部过程可追踪、可回滚、可继续扩展。

## 2. 当前系统边界

### 2.1 RG660MK-EU

- 系统：OpenWrt 5.15.134 / aarch64
- 当前 PicoClaw 落点：
  - `/data/ai_cpe/picoclaw/current/picoclaw`
  - `/data/ai_cpe/picoclaw/.picoclaw/config.json`
  - `/data/ai_cpe/picoclaw/.picoclaw/workspace`
- 当前 Gateway：
  - 监听 `127.0.0.1:18790`

### 2.2 Ubuntu 主机

- Immich 根目录：`/home/jedyying/immich`
- Immich Web/API 入口：`http://127.0.0.1:2283`
- 宿主机本地 PicoClaw 上游代理：
  - `127.0.0.1:18080`
  - 上游转发到 `https://ai.phicotek.com/api/llmrouter/v1/chat/completions`

## 3. 设计结论

### 3.1 PicoClaw 有效部署路径

由于 RG660MK 当前没有可用 WAN 默认路由，且 DNS 指向本地回环地址，设备本机无法稳定直接访问外部 LLM 服务。

因此本日采用的有效架构是：

1. RG660MK 上运行 PicoClaw。
2. RG660MK 使用 `openai` provider。
3. `api_base` 指向设备本地 `http://127.0.0.1:18080/v1`。
4. 通过 `adb reverse tcp:18080 tcp:18080` 将该端口映射到 Ubuntu 主机。
5. Ubuntu 主机上的本地代理再转发到 `ai.phicotek.com`。

这是当前最短、最稳、已验证成功的路径。

### 3.2 Immich 有效部署路径

Immich 不部署在 RG660MK 上，继续保留在 Ubuntu 主机：

1. Ubuntu 主机负责 Immich Server、Postgres、Valkey、Machine Learning。
2. RG660MK 只通过 HTTP API 使用 Immich。
3. 上传、查询、检索均在主机侧完成存储与推理。

## 4. 今日落地的关键组件

### 4.1 RG660MK 侧

- PicoClaw 主程序
- PicoClaw 配置目录
- PicoClaw Gateway
- Anthropic 认证状态
- 宿主机代理接入后的 OpenAI 兼容访问路径

### 4.2 宿主机侧

- Immich 服务
- `Phicotek` OpenAI 兼容代理
- `adb reverse`
- PicoClaw/Immich 脚本与交付文档

## 5. 当前限制

### 5.1 RG660MK 直连外网未完成

已确认问题：

- `/etc/resolv.conf` 仅有 `127.0.0.1` 与 `::1`
- 无可用 WAN 默认路由
- 本地 `vLLM/Ollama` 状态页显示存在，但实际无进程监听对应端口

### 5.2 目前依赖 ADB 反向端口映射

当前 PicoClaw 可用，但依赖：

- 宿主机在线
- `adb reverse` 保持有效
- 宿主机代理保持运行

## 6. 后续正式化方向

1. 修复 RG660MK WAN/DNS，使设备可直接访问外部模型。
2. 将当前宿主机代理方案转为备份方案，而不是主路径。
3. 把 Immich 上传脚本接入 PicoClaw tools 或事件链路。
4. 补齐 Gateway 自启动、代理自启动和回滚脚本。
