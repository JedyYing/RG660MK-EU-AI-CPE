# 阶段A 中期报告 — 解析链自证 (设备实测前)
生成: 2026-08-28T14:03:21+08:00 · QRIBuddy

## 已完成 (无需密码, 全部通过)
1. 路由证明: ip -4 route get 192.168.1.1 => via ... dev enx9c69d3c6b35d, 不经任何 wg 接口。见 report/route_proof.txt。
2. 工具链: libx264 存在; aarch64-linux-gnu-gcc 存在; TShark 3.6.2; tcpdump 齐全。
3. aarch64 静态 UDP sink 已交叉编译 (bin/rg_udp_sink, ELF ARM aarch64 statically linked)。
4. 本地环回自测: FFmpeg(与实测同参数) 生成 H.264/RTP -> 用户态收包 5924 包 -> pcap。
5. 交叉验证: 自研解析器 vs TShark 完全一致。
   - 包数 5924 = 5924; SSRC 0xE8B88D1B; PT96; 丢包 0; 乱序 0; 重复 0。
   - 序列号扩展正确 (first=208 last=6131 expected=received=5924)。
   - NALU: SPS=12 PPS=12 IDR=96 nonIDR=2784 STAP-A=451 FU-A重组=861 缺片=0 参数集OK 关键帧可见。
   - RFC3550 抖动: mean=0.073ms p95=0.199ms (环回, 近乎无抖动, 合理)。
6. 处理延迟(纯解析, 环回样本 5924): P50=0.006ms P95=0.010ms P99=0.012ms (远优于20-80ms目标;
   注: 这是解析计算耗时, 实测还需叠加 SSH 流式传输, 待设备实测补全)。

## 结论
"RG抓包->SSH流式->PC解析->指标输出" 链条中的**解析与指标算法部分已被证明正确**:
RTP序列/丢包/抖动、H.264 NALU/SPS/PPS/IDR、STAP-A拆分、FU-A重组均验证通过, 无注入故障时不虚报丢包/乱序。

## 待你输密码后由脚本自动完成 (设备实测)
- run_phaseA_capture.sh: 双点抓包(PC enx9c69d3c6b35d + RG eth1) + 40s FFmpeg 发送 + sink 部署/清理。
- run_phaseA_analyze.sh: 双 pcap 对比 + capinfos内核丢包 + 三类丢包区分 + 实时延迟。

## 边界声明
本阶段仅验证解析算法与实时处理链, 不声称验证5G链路(阶段B, PENDING)。
所有 pcap/SDP/日志权限 0600, 仅存本机。未执行 ssh-copy-id, 未改 authorized_keys。
