# 新疆口音优化 · 上机部署与自测记录（RG660MK CPE）

- 日期：2026-09-20 傍晚（18:17–18:40）
- 设备：RG660MK CPE（OpenWrt / aarch64，SSH root@192.168.1.1）
- 依据：《RG660MK 智能音箱新疆口音识别优化设计 V1.0》§6.1 / §6.3 / §9
- 代码提交：`c12de41`（口音优化并入 Sep16-18 设备版）+ `32e75b6`（唤醒路径 prompt 修复）

## 一、部署内容（已上机）

| 文件 | 设备位置 | md5 | 说明 |
|---|---|---|---|
| voice_assistant.py | /data/ai_cpe/ | `cef5cf07…` | 合并版：Sep16-18 全部修复 + 口音优化 + 唤醒路径修复 |
| accent_correct.py | /data/ai_cpe/ | `edf9a744…` | 新增（口音纠错 + 热词偏置） |
| 旧版备份 | /data/ai_cpe/voice_assistant.py.bak_20260920_preaccent | `cd70d572…` | Sep16-18 版，可随时回滚 |
| 自测脚本 | /data/ai_cpe/accent_selftest_device.py | — | 逻辑回归自测，可重复运行 |

服务重启：`/etc/init.d/voice_assistant restart` → 新进程正常，日志
`voice_assistant started: rec=plughw:2,0 play=plughw:2,0 vad=True`。

> 合并说明：口音优化（cabb03b）原先基于 3229c3e 旧基底，本次已将其与设备
> 已验证但未提交的 Sep16-18 改动（实时行情 / 拒绝即终答 / TTS 缓存 / ASR 超时）
> 合并，二者均在最终版本中，互不缺失。

## 二、设备端自测（全绿）

- `py_compile` 通过；`accent_correct.py selftest` 12 PASS / 0 FAIL
- `accent_selftest_device.py`：37 项断言 **ALL PASS**（口音纠错 9、classify 端到端 4、
  标准普通话回归 8、新疆地名天气 7、误纠红线 6、行情不回归 1、热词 2）
- 本机 pytest：38 passed / 83 subtests；验收脚本 27/27 ALL GREEN

关键抓取（设备真实运行）：

```
[ACCENT] '打开客厅登' -> '打开客厅灯' (dist=0)  → DEVICE_CONTROL / control_bulb / on
[ACCENT] '关闭客厅登' -> '关闭客厅灯' (dist=0)  → control_bulb / off
[ACCENT] '打开卧市登' -> '打开卧室灯' (dist=0)  → control_bulb / on
打开窗连 -> 打开窗帘（文本纠对；当前无窗帘工具，按预期走 LLM 兜底）
乌鲁木齐/喀什/伊犁/克拉玛依/吐鲁番/库尔勒/哈密 → WEATHER + 城市提取正确
```

## 三、whisper --prompt 时延实测（设计 §9 待验证项，本次量化）

设备 whisper.cpp 1.9.3-dev / ggml-base / Cortex-A55，交叉顺序重复测量：

| 条件 | 3.55s 短音频 | 5.61s 音频 |
|---|---|---|
| 无 prompt | 2.43 / 2.44s | 2.57 / 2.58s |
| 全量热词 prompt（144 字） | 4.67 / 4.72s | 5.05 / 5.06 / 5.08s |

**结论：热词 prompt 是固定成本，每次转写 +2.2~2.5s（约为基线 2 倍），与音频长度无关。**

由此完成两处调整（已在 32e75b6 提交并上机）：

1. **唤醒/待机路径停用 prompt**（`transcribe(use_prompt=False)`）——
   唤醒词不在热词表内，待机对每条环境语音都要转写一次，加偏置只有延迟与
   CPU 空耗。实测：小皮 2.23~2.25s（无）vs 4.40~4.45s（有），两者均正确唤醒；
   停用后唤醒恢复原基线速度。
2. **命令路径 prompt：当晚实测后改为默认关闭**（证据见 §六）。机制保留在代码中
   （`VOICE_ASR_PROMPT=1` 一键启用），待真实口音录音 A/B 通过后再开。

## 四、发现与边界（需真机测试关注）

1. **解码循环**：少量音频上 whisper base 会陷入重复循环（合成 TTS "打开卧室灯"
   无 prompt 时输出 `打开握时登`×36，6.8s；"打开客厅灯" 样本循环且输出截断在
   半个 UTF-8 字符上 → `UnicodeDecodeError`）。transcribe() 捕获后返回空，
   等效超时 → 走"请再说一下，我没听清"，安全降级。
2. **prompt 与循环的互动**：带 prompt 时个别循环样本会越过 `ASR_TIMEOUT=8s` →
   空结果（超时防护正常工作）；但也有反向样本（同一音频无 prompt 循环、
   带 prompt 反而正确 4.6s 解出）。真实录音下的循环率需统计。
3. 以上均为合成音频观察，**真实新疆口音录音的 CER/命令准确率/循环率待按设计 §7
   验收集验收**；混淆规则阈值仍为待校准初值。

## 五、测试指引（对着设备说话时）

```sh
# 实时看日志（另开终端执行）
ssh root@192.168.1.1 "logread -f" | grep -iE "ACCENT|WAKE|ASR_TEXT|listen|tool"

# 复跑设备端逻辑自测
ssh root@192.168.1.1 "cd /data/ai_cpe && /data/hermes/venv/bin/python accent_selftest_device.py"

# 回滚（如需）
ssh root@192.168.1.1 "cp /data/ai_cpe/voice_assistant.py.bak_20260920_preaccent /data/ai_cpe/voice_assistant.py && /etc/init.d/voice_assistant restart"
```

测试建议：① 唤醒速度（"小皮"→"在呢"）；② 口音读法的灯类命令（关注 `[ACCENT]` 行）；
③ 标准普通话回归（灯/时间/天气）；④ "乌鲁木齐今天天气怎么样"；
⑤ 若"请再说一下"偏多，记录具体话术样本（可能是解码循环）。

## 六、当晚真实语音上机 A/B（20:00-20:15）—— 热词 prompt 判定为负面

用户实测「你好小皮」多次失败/答非所问，抓取真实录音并做**同一音频**转写对比：

| 模式 | 转写结果 | 影响 |
|---|---|---|
| 无 prompt | `你好下題` | 接近唤醒词，可唤醒 |
| 全量热词 prompt | `阿梅，鲁，河田` | 被热词表（乌鲁木齐/和田等）劫持 → 误落天气 → "你想查哪个城市" |
| 去 query 组 prompt | `''`（循环超时） | 仍不稳定 |

另：两次唤醒转写为空 `''`；一次 `你好下皮×75` 循环输出被整段当命令送后端
（用户听到"问答服务暂时没有返回结果"）。

修正（提交 `fix(voice): 唤醒可用性修复`）：
- `strip_wake()` 全量收敛：循环唤醒词全部剥离，纯唤醒呼叫回"在呢/我在听"，不再送后端；
- 热词 prompt 默认关闭（`ASR_HOTWORD_PROMPT`；env `VOICE_ASR_PROMPT=1` 可开）；
- 唤醒词变体补入实测到的「下题」；
- 待查：个别转写为空的成因（现场抓录分析中）。
