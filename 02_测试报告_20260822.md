# RG660MK-EU AI CPE 测试报告

- 日期：2026-08-22
- 测试范围：RG660MK-EU PicoClaw、Immich、宿主机代理、ADB 链路

## 1. 测试环境

### 1.1 设备

- RG660MK-EU
- 系统：OpenWrt 5.15.134
- 架构：aarch64

### 1.2 主机

- Ubuntu 主机
- Immich：`http://127.0.0.1:2283`

## 2. 测试项与结果

### 2.1 ADB 连通

- 项目：`adb devices -l`
- 结果：PASS
- 关键结果：
  - 设备序列号 `0123456789ABCDEF`
  - 设备状态 `device`

### 2.2 PicoClaw 二进制可执行

- 项目：`picoclaw version`
- 结果：PASS
- 关键结果：
  - 版本 `0.3.1`
  - 二进制位于 `/data/ai_cpe/picoclaw/current/picoclaw`

### 2.3 PicoClaw 配置目录存在

- 项目：`picoclaw status`
- 结果：PASS
- 关键结果：
  - `config.json` 存在
  - `workspace` 存在

### 2.4 Anthropic 官方认证写入

- 项目：`picoclaw auth login --provider anthropic`
- 结果：PASS
- 关键结果：
  - `auth status` 显示 `anthropic`
  - `Status: active`

### 2.5 RG660MK 本地 LLM 回退链路

- 项目：PicoClaw 直接访问本地 `vLLM` 与 `Ollama`
- 结果：FAIL
- 失败原因：
  - `http://localhost:8000/v1/chat/completions` 连接被拒绝
  - `http://localhost:11434/v1/chat/completions` 连接被拒绝
- 结论：
  - 本地端点未真正运行，不可作为当日有效上游

### 2.6 RG660MK 外部模型直连

- 项目：PicoClaw 直接访问 `ai.phicotek.com`
- 结果：FAIL
- 失败原因：
  - DNS 解析失败
  - 设备侧无有效 WAN 默认路由

### 2.7 宿主机上游代理直连

- 项目：宿主机直接调用 `https://ai.phicotek.com/api/llmrouter/v1/chat/completions`
- 结果：PASS
- 关键结果：
  - 返回 `HTTP 200`
  - 返回模型结果，问题 `1+1` 的答案为 `2`

### 2.8 RG660MK PicoClaw 经宿主机代理问答

- 项目：RG660MK PicoClaw 最小文本请求
- 结果：PASS
- 测试问题：
  - `请只用一句中文回答：1+1等于几？`
- 返回结果：
  - `1+1等于2。`
- 有效链路：
  - RG660MK PicoClaw
  - `adb reverse`
  - Ubuntu 主机本地代理
  - `ai.phicotek.com`

### 2.9 PicoClaw Gateway 启动

- 项目：启动 `picoclaw gateway --host 127.0.0.1`
- 结果：PASS
- 关键结果：
  - 日志显示 `Gateway started on 127.0.0.1:18790`

### 2.10 PicoClaw Gateway 健康检查

- 项目：宿主机经 `adb forward` 调用 `http://127.0.0.1:18790/health`
- 结果：PASS
- 关键结果：
  - 返回 `HTTP 200`
  - 返回：
    - `status: ok`
    - `pid: 20736`

### 2.11 Immich Web 服务在线

- 项目：`GET /server-info`
- 结果：PASS
- 关键结果：
  - 返回 `HTTP 200`

### 2.12 Immich 搜索 API

- 项目：`POST /api/search/metadata`
- 结果：PASS
- 关键结果：
  - 返回 `HTTP 200`
  - API key 可用

### 2.13 Immich 上传 API

- 项目：`POST /api/assets`
- 结果：PASS
- 关键结果：
  - 返回 `HTTP 201`
  - 测试上传资产 ID：
    - `f11db6e1-4bb5-4cbd-94b1-759462dafb5a`

## 3. 总结

### 3.1 今日通过项

- ADB 连通
- PicoClaw 二进制运行
- PicoClaw 配置存在
- PicoClaw 最小文本问答
- PicoClaw Gateway 启动与健康检查
- Immich Web/API 读写

### 3.2 今日未通过项

- RG660MK 本机 `vLLM`
- RG660MK 本机 `Ollama`
- RG660MK 直连外部 LLM

### 3.3 当前可用结论

RG660MK-EU 上的 PicoClaw 已可用，但当前依赖宿主机代理和 `adb reverse`。Immich 已可正常访问和写入。
