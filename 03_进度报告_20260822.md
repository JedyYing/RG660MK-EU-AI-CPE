# RG660MK-EU AI CPE 进度报告

- 日期：2026-08-22
- 状态：进行中

## 1. 今日完成

### 1.1 文档与交付整理

- 整理了 RG660MK-EU AI CPE 迁移资料
- 建立了 `AI_CPE_Demo` 交付目录
- 生成了迁移矩阵、脚本、配置模板和校验清单

### 1.2 PicoClaw 部署推进

- 确认 RG660MK 上 PicoClaw 二进制存在并可执行
- 确认 onboard 配置已生成
- 通过 `adb` 实机接入设备
- 完成 Anthropic 认证写入
- 排除了本机 `vLLM/Ollama` 回退路线
- 建立宿主机 OpenAI 兼容代理
- 通过 `adb reverse` 将设备接入宿主机代理
- 实机完成 PicoClaw 文本问答
- 启动 PicoClaw Gateway 并通过健康检查

### 1.3 Immich 部署推进

- 确认主机侧 Immich 在线
- 实测搜索 API 成功
- 实测上传 API 成功

## 2. 当前状态

### 2.1 PicoClaw

- 状态：可用
- 当前有效模式：
  - RG660MK 本机运行 PicoClaw
  - 宿主机代理承担外部模型访问
  - `adb reverse` 负责端口映射

### 2.2 Immich

- 状态：可用
- 当前有效模式：
  - 继续运行在 Ubuntu 主机
  - API 已验证读写正常

## 3. 仍未完成

### 3.1 RG660MK 自身独立出网

当前 PicoClaw 并不是靠 RG660MK 自己访问外部模型，而是依赖宿主机代理。

未完成项：

- 修复 RG660MK 的 WAN 默认路由
- 修复 RG660MK 的 DNS 解析
- 让 RG660MK 直接访问 `ai.phicotek.com`

### 3.2 PicoClaw 正式服务化

虽然 Gateway 已经启动成功，但还未做正式持久化：

- 开机自启
- 自动恢复
- 宿主机代理联动启动
- 一键停止与回滚

### 3.3 Immich 接入 PicoClaw 自动链路

Immich API 已经可用，但还没有接回：

- PicoClaw tools
- 抓拍上传事件链
- 检索查询入口

## 4. 风险

### 4.1 当前 PicoClaw 依赖宿主机

若宿主机关机、代理退出、`adb reverse` 失效，则 PicoClaw 外部大模型能力会中断。

### 4.2 RG660MK 网络栈未完成基线修复

当前不是完整的独立 CPE AI 形态，而是“设备侧 Agent + 宿主机出网代理”的过渡架构。

## 5. 下一步建议

1. 优先修复 RG660MK WAN/DNS，替代 `adb reverse` 代理方案。
2. 将宿主机代理保留为回退方案。
3. 把 Immich 上传与查询能力接入 PicoClaw。
4. 做 PicoClaw 与代理的正式服务化和回滚脚本。
